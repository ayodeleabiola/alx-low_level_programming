#include "main.h"
/**
 *main - check the alx student code
 *Return: Always 0
 */
int main(void)
{
	int *str;

	str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
	print_rev(str);
	return (0);
}
