#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Is always the starting point
 *
 * Return: Always 0(Abiola)
 */
int main(void)
{
	char lower;

	for (lower = 'a'; lower <= 'z'; lower++)
	{
		putchar(lower);
	}
	putchar('\n');
	return (0);
}
